{% extends 'base.html.twig' %}

{% block title %}Imggallery{% endblock %}

{% block body %}
    <h1>Imggallery</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imggallery.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ imggallery.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ imggallery.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_imggallery_index') }}">back to list</a>

    <a href="{{ path('admin_imggallery_edit', {'id': imggallery.id}) }}">edit</a>

    {{ include('admin/imggallery/_delete_form.html.twig') }}
{% endblock %}
